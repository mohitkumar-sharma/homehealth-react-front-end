import React from 'react';
import Default from './layout/Default';

const Inner = (): JSX.Element => {
  return (
    <Default>
      Inner page content here
    </Default>
  );
}

export default Inner;
